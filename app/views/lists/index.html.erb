<div class="cover mb-4 d-flex align-items-center justify-content-center bg-danger"> <!-- div do meu titulo-->
  <div class="text-center">
  <h2>Save any Kind of Movies</h2>
  </div>
</div>

<div class="container bg-danger p-3"> <!-- div container do restante da página do list index-->
  <div class="d-flex justify-content-center align-items-center m-2"> <!-- alinhamento dos elementos que estiverem dentro do div do link -->
    <h4>My list of movies</h4>
    <%= link_to 'Create a list', new_list_path %>
  </div>
  <div class="row"> <!-- alinhamento dos demais elementos do index-->
    <% @lists.each do |list| %>
      <%= list.name %>
    <% end %>
  </div>
</div>